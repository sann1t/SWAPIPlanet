<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.swapiapi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.peoples.info.PeopleActivityInfo"></activity>
        <activity android:name=".views.planets.info.PlanetActivityInfo"></activity>
        <activity android:name=".views.species.info.SpecieActivityInfo"></activity>
        <activity android:name=".views.starships.info.StarShipActivityInfo"></activity>
        <activity android:name=".views.vehicles.info.VehicleActivityInfo"></activity>
        <activity android:name=".views.films.info.FilmActivityInfo"></activity>
        <activity android:name=".views.starships.StarShipActivity" />
        <activity android:name=".views.species.SpecieActivity" />
        <activity android:name=".views.films.FilmActivity" />
        <activity android:name=".views.peoples.PeopleActivity" />
        <activity android:name=".views.vehicles.VehicleActivity" />
        <activity android:name=".views.planets.PlanetActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>